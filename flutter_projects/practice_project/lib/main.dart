import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: const Text('Expende Tracker'),
          // toolbarHeight: 10,
          backgroundColor: Colors.blue,
        ),
        body: const Center(
          child: Text(" "),
        ),
      ),
    ),
  );
}
